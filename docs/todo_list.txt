TODO:
    1) Реализовать индексы в миграторе
    2) Реализовать миграции по сгенерированным SQL файлам в миграторе
    3) Закрыть "TODO" в миграторе
    4) Вынести сервис console из мигратора на уровень pkg
    5) Добавить ban type в модель [user] ban
    6) реализовать отдельные провайдеры для разных БД в миграторе. Сейчас работает только Postgres
    7) В миграторе есть баг с большим количеством фикстур - фейкер генерирует одинаковые ID
    8) В миграторе реализовать запись фикстур в один итоговый SQL файл (сейчас пишет каждую фикстуру в отдельный файл)